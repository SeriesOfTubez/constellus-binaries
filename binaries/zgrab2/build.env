# zgrab2 — application-layer banner-grab scanner (zmap). Built from pinned source.
# Bump VERSION to upgrade; the pipeline re-runs govulncheck + Trivy before publishing.
REPO=https://github.com/zmap/zgrab2
VERSION=v1.0.0
PKG=./cmd/zgrab2
BIN=zgrab2
GO_VERSION=1.25
